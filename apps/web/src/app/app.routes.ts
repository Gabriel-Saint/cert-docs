import type { Route } from '@angular/router';

/**
 * Rotas do app (preenchidas nas próximas etapas, com lazy loading):
 *
 *   /auth/login       tela de login
 *   /auth/register    tela de cadastro
 *   /documents        lista de documentos e download do PDF   (autenticado)
 *   /admin/documents  gestão de documentos                    (ADMIN)
 *   /admin/users      lista de usuários                       (ADMIN)
 */
export const appRoutes: Route[] = [];
